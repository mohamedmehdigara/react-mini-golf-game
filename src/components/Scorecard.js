import React from 'react';
import styled from 'styled-components';

const StyledScorecard = styled.div`
  /* Add your scorecard styling here */
`;

const Scorecard = () => {
  return <StyledScorecard>Scorecard</StyledScorecard>;
};

export default Scorecard;
